# تقرير الاختبار الفعلي الشامل — 2026-07-03

> اختبار حقيقي عبر متصفح (كمبيوتر 1280px + جوال 390px) على **palmkit.app** بحساب
> `maxok99x8@gmail.com`، مع بناء **3 مشاريع بالتوازي في 3 محادثات جديدة**
> (بسيط/متوسط/معقّد)، وتحديث الصفحات، وإغلاق التبويب والعودة، ومراقبة قاعدة
> البيانات (Supabase) والعامل الخلفي (Oracle Worker) مباشرة.

---

## 1. نتائج البناء الثلاثة (الخلاصة)

| المشروع | النوع | النتيجة | الزمن | ملاحظات |
|---------|------|---------|-------|---------|
| **Lithos** (hero + spotlight، برومبت قوي) | react | ✅ ready + build verified | **4:41 د** | 10 ملفات، Builder + Tester، `npm run build` نجح فعليًا في E2B، والمعاينة طابقت التصميم المطلوب بدقة عالية |
| **Ledgerly** (لوحة مالية متوسطة) | react | ✅ ready + build verified | **~14 د** | 19 ملف / 76KB. الـTester وصل حد الخطوات (6) وأكمل |
| **Nexera** (إدارة مشاريع معقّد) | react | ✅ ready لكن **بدون تحقق** | **16:09 د** | 24 ملف / 124KB — **sandbox الـE2B مات عند دقيقة 16 (TTL)** فكل أوامر الشل بعدها فشلت (`cwd /home/user/project does not exist`) وتخطّى الـTester بصمت ("1 agents") |

**الاستنتاج:** خط الأنابيب (enqueue → poll → Builder → Tester → R2 → manifest) يعمل
فعليًا وبالتوازي، والمشكلة الحقيقية ليست في "هل يبني" بل في **ما بعد البناء
(العرض)** وفي **تحقّق البناءات الطويلة**.

---

## 2. الأخطاء المؤكدة بالجذر (وكلها أُصلحت في هذه الجولة)

### أ) المعاينة "تشتغل وتنقطع وترجع" — أخطر خطأ
- **الجذر:** بروكسي `/preview/` على Cloudflare Pages يمرر HTTP لكن ردّ الـ101
  الخاص بـWebSocket كان **يفقد ترويسة `Sec-WebSocket-Protocol`**. عميل Vite
  يطلب البروتوكول `vite-hmr`، وحسب مواصفة WebSocket المتصفح **يقتل** أي اتصال
  لا يعيد البروتوكول. النتيجة الملاحظة حرفيًا في الكونسول:
  `WebSocket handshake failed` → `server connection lost. Polling for restart...`
  → الـping ينجح عبر HTTP → Vite **يعيد تحميل الـiframe** → حلقة وميض لا نهائية.
- **الإصلاح:** إعادة ترويسة البروتوكول في ردّ الـ101 (`functions/preview/[[path]].ts`).

### ب) لا عرض تلقائي بعد اكتمال البناء
- **الجذر:** قرار قديم مكتوب في `use-worker-sandbox.ts` ("NO AUTO-LAUNCH") كان
  يعطّل الإطلاق عمدًا، فيبقى المستخدم أمام تبويب Code ولازم يكتشف بنفسه
  Workspace → Preview → Launch Preview.
- **الإصلاح:** عند `ready_for_preview` (البناء **متحقق منه** أصلاً في الخادم)
  يُطلق sandbox الـE2B تلقائيًا، وعند جاهزيته يفتح الـWorkspace ويتحول للعرض
  مباشرة — على الكمبيوتر والجوال. التطبيقات الساكنة (blob) نفس السلوك.

### ج) الـWorkspace على الجوال "طبقة تطفو فوق الدردشة"
- **الجذر:** لوحة الـworkbench كانت بطاقة `fixed` بحواف دائرية وإزاحات
  (`top+1.2rem`, `bottom-6`) فتظهر المحادثة خلفها من الأطراف.
- **الإصلاح:** تحت 640px تملأ الشاشة من الهيدر إلى الدوك بلا حواف — شاشة
  مستقلة حقيقية.

### د) قائمة المحادثات في الجوال تطلع من الأسفل + عناوين غير مقروءة
- **الجذر:** الهامبرجر يفتح `ProjectSwitcherDrawer` بأنيميشن `y:100%` (bottom
  sheet)، وألوانه مبنية على متغيرات CSS مصممة للثيم الداكن فقط فكانت العناوين
  **غير مرئية** في الوضع الفاتح، وما فيه إغلاق بـEscape.
- **الإصلاح:** درج جانبي من **اليسار** (85% حتى 340px)، ألوان
  `dark:` القياسية نفسها المستخدمة في سايدبار الكمبيوتر، وEscape يغلقه.

### هـ) السحب (stream) يبدو "hardcoded" ونص متراكب مشوّه
- **الجذر:** مسار العامل يحدّث `message.content` عند كل poll لكنه **ما كان
  يحدّث `message.parts`** — والـai-sdk يعرض الرسالة من parts، فتتكدس النسخ
  القديمة وتظهر متلاصقة: `Building project…project…** · 1 reasoning stepne…`.
- **الإصلاح:** تحديث parts مع content في نفس العملية.

### و) الجوال "يخطفك" للـWorkspace في منتصف البناء
- **الجذر:** أول ملف يُكتب → `showWorkbench=true` → `MobileShell` يحوّل التبويب
  قسرًا. وأيضًا زر Workspace كان يفرض تبويب Code لأن فحص "هل يوجد معاينة" يقرأ
  مخزن WebContainer القديم (فارغ دائمًا على مسار E2B).
- **الإصلاح:** على الجوال لا يُفتح الـWorkspace تلقائيًا أثناء البناء (السحب
  الحي داخل الدردشة يكفي)، ويُفتح تلقائيًا **عند جاهزية العرض** فقط؛ وزر
  Workspace يحافظ على آخر تبويب كنت عليه.

### ز) السايدبار في الكمبيوتر يغطي صندوق الكتابة + "No previous conversations"
- **الجذر:** لوحة ثابتة 340px فوق المحتوى بلا إزاحة للتخطيط، وقائمتها تُقرأ
  مرة واحدة عند الفتح فقط (وكان دائم الفتح ⇒ لا تحديث أبدًا حتى مع 3 محادثات
  جارية).
- **الإصلاح:** متغير `--sidebar-width` يدفع الدردشة والـworkbench فعليًا
  (والأيقونة في الهيدر تطويه/تفتحه)، والقائمة تتحدث عند focus + كل 15 ثانية.

### ح) تسريب معاينة محادثة داخل محادثة أخرى
- **الجذر:** كوكي `pf_preview` واحدة عامة لكل الموقع — فتح محادثة B كان يستعيد
  sandbox محادثة A.
- **الإصلاح:** الكوكي أصبحت `sid:port:chatId` ولا يُعاد الاتصال إلا لنفس
  المحادثة.

### ط) (العامل الخلفي) البناءات الطويلة تفقد الشل عند الدقيقة 16
- **الجذر:** TTL مطلق (16 دقيقة من الإنشاء) لـsandbox E2B؛ بناء Nexera تجاوزه
  فماتت البيئة تحت يد الوكيل وتخطّى التحقق بصمت.
- **الإصلاح:** TTL **متجدد** مع كل أمر + عند موت الـsandbox يُعاد إنشاؤه
  وتُدفع الملفات كاملة ويُعاد الأمر مرة واحدة (`e2b-runner.ts`).

---

## 3. ما تم نشره في هذه الجولة

- الالتزام `f9df22d` (12 إصلاحًا أعلاه) — دُفع إلى `main`:
  - ✅ **Oracle Worker** انتشر عبر GitHub Actions (run 66 — success).
  - ✅ **Cloudflare Pages** (production) — انظر حالة النشر في نهاية الجلسة.
- تم التحقق محليًا قبل الدفع: `tsc` نظيف، `eslint` نظيف، `pnpm build` ناجح،
  وworker يمر عبر `tsc` بدون أخطاء.

---

## 4. مقارنة معمارية: Palmkit مقابل بيئة تطوير وكيلية (مثل التي أعمل بها)

| المحور | بيئتي كوكيل | Palmkit اليوم | الفجوة/التوصية |
|--------|-------------|---------------|-----------------|
| **حلقة التحقق** | كل تعديل يُبنى ويُشغَّل ويُفحص قبل التسليم؛ الفشل يعيد الحلقة تلقائيًا | البناء الأول متحقق (Tester + build حقيقي) لكن **التعديلات (edits) بلا أي تحقق**، والـbuild-checker المستقل معطّل (`|| true`) | أعِد تفعيل بوابة build الحقيقية للجميع، ومرر التعديلات على نفس فحص E2B |
| **أدوات الوكيل** | شل كامل + قراءة/كتابة/بحث + تشغيل التطبيق نفسه ومشاهدة سلوكه | الـBuilder يملكها، لكن **مسار Edit استدعاء نصي واحد بلا أدوات** | وحّد المسارين: التعديل = نفس الوكيل بنفس الأدوات مع سياق المشروع |
| **الذاكرة بين الجلسات** | ملفات ذاكرة/CLAUDE.md تحدد السياق | يوجد `.palmkit/` memory — جيد | استغلها في الـEdit prompt (اليوم شبه مهملة) |
| **إظهار العمل** | سرد مباشر + أدلة (مخرجات أوامر) | BuildStream جيد جدًا فعليًا (خطط/ملفات/أوامر/مدد) | تمت إزالة التشوهات؛ أضف "لماذا" مختصرة لكل مرحلة |
| **تسليم النتيجة** | تشغيل فعلي وإثبات أن التطبيق يعمل | كان يتوقف عند "الملفات جاهزة" | أُصلح: الآن التشغيل والعرض تلقائيان |
| **عزل المحادثات** | كل جلسة معزولة تمامًا | كانت الكوكي/المخازن عالمية | أُصلح للكوكي؛ راقب بقية المخازن العالمية (buildStatus/previewFiles) عند فتح محادثتين بالتوازي في نفس التبويب |

**الحكم:** المعمارية (Pages + queue في Supabase + عامل يسحب + E2B + R2) **سليمة
ومناسبة للتوسع الأفقي** وليست "ضعيفة". الضعف كان في: طبقة العرض بعد البناء،
اتساق تجربة الجوال، ومسار التعديل غير المتحقق.

---

## 5. خطة التحسين على مراحل

### المرحلة 1 — تمّت اليوم (منشورة)
كل بنود القسم 2.

### المرحلة 2 — موثوقية المحرك (أسبوع)
1. **تحقق التعديلات:** مرّر ناتج `generateEdit` على فحص build في E2B قبل
   `ready_for_preview`، أو الأفضل: حوّل التعديلات للـBuilder الوكيلي نفسه.
2. **أعد تفعيل build-checker** كبوابة صلبة (أزل `|| true` في job-processor).
3. **رفع حد خطوات الـTester** (6 → 10) للبناءات >15 ملف، مع ميزانية زمن منفصلة.
4. **نموذج أسرع للتعديلات** (غير reasoning) + إخراج diff/patch بدل إعادة كتابة
   الملفات كاملة — هذا وحده يقصّ زمن التعديل من ~2 دقيقة إلى ثوانٍ ويوفر
   التوكنز الأكبر في النظام.

### المرحلة 3 — تجربة الجوال (أسبوع)
1. **تصغير الـcomposer:** صفّا المزود/النموذج/API key يطوون خلف chip واحد —
   اليوم يأكلون نصف الشاشة.
2. شريط الحالة السفلي (RemotePreviewTrigger) يندمج داخل BuildStream بدل طبقة
   عائمة فوق صندوق الكتابة.
3. زر "شاهد التطبيق" داخل رسالة "Build complete" في الدردشة (fallback دائم
   للتشغيل التلقائي).

### المرحلة 4 — السرعة (أسبوعان)
1. **زمن أول عرض:** أنشئ sandbox العرض **أثناء** البناء (prewarm عند
   `build_check_passed`) بدل بعد `ready_for_preview` — يوفر 20–60 ثانية.
2. **Realtime بدل polling** حيث أمكن (القناة موجودة أصلًا؛ قلّل poll الـ2s إلى
   heartbeat فقط).
3. قلّص حزمة الواجهة: أزل بقايا WebContainer/Electron من مسار الويب
   (code-splitting للـeditor وxterm — موجود جزئيًا).
4. صفحة `/chat/:id` أثناء بناء جارٍ تعلّقت >60 ثانية في اختبارنا عند التحديث —
   راقبها بعد نشر إصلاحات اليوم؛ إن تكررت فالسبب المرجح انتظار
   fetch للملفات قبل الـhydration.

### المرحلة 5 — التوكنز (مستمر)
- diff-edits (أعلاه) + سقف `maxTokens` متناسب مع حجم المشروع بدل 64k ثابتة.
- تجميع أحداث reasoning (موجود) + عدم إرسال الأحداث الكاملة مع كل poll
  (أرسل `since=seq`).
- ذاكرة `.palmkit/decisions` تُحقن ملخصةً (سطور، ليس ملفات كاملة) في برومبت
  التعديل.

---

## 6. ملاحظات أمنية عاجلة (خارج نطاق الكود)

أثناء الجلسة زوّدتني بمفاتيح حساسة في نص المحادثة، ومنها **Service Role Key
لـSupabase وGitHub PAT وCloudflare token وE2B ومفتاح تشفير المفاتيح وخاص SSH**.
هذه يجب اعتبارها **مكشوفة الآن**:
1. دوّر (rotate) فورًا: GitHub PAT، Cloudflare token، Supabase service key،
   E2B key، `API_KEY_ENCRYPTION_KEY` (مع إعادة تشفير `user_api_keys`)، وزوج SSH.
2. لا تضع service key في أي مكان يصل للمتصفح — تحقق أن `/api/sb` لا يمررها.
